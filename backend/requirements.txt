fastapi==0.115.0
uvicorn[standard]==0.32.0
scikit-learn==1.5.2
pandas==2.2.3
numpy>=1.26.4
websockets==13.1
python-multipart==0.0.12

# ── Audit additions (2026-04-14) ──────────────────────────────────────────
# Future roadmap items (commented to save weight)
# asyncpg==0.30.0
# databases[postgresql]==0.9.0
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4

# ONNX edge inference (future-proofing — export sklearn models to ONNX)
# onnxruntime==1.20.1         # Uncomment when real models are added
# skl2onnx==1.17.0            # Uncomment when real models are added

# Federated learning (Phase 3 roadmap)
# flwr==1.9.0                  # Uncomment for federated IDS implementation
